type Pessoa = {
    nome: string;
    idade: number;
}

const pessoas: Pessoa[] = [
    { nome: 'João', idade: 30 },
    { nome: 'Maria', idade: 25 }
    ]

const numeros: number[] = [10, 2, 100, 5]

const frutas = ['banana', 'maçã', 'laranja', 'abacaxi']

